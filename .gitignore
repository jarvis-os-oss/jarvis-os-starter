# Secrets and local runtime state (never commit these)
.env
*.env.local
.cockpit_token
*.key
*.pem
*.token
secrets/
.secrets/
# Owner's real denylist terms for the PII scanner (the .example is tracked).
.scan_denylist.local
# Your chosen agent names (the .example is tracked; run scripts/name_agents.py).
agents/names.local

# Runtime artefacts
logs/
*.log
*.db
*.db-wal
*.db-shm
*.flag
*.bak
last.json

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# OS / editor
.DS_Store
.idea/
.vscode/
